# ============================================================
# SemantiCite.ai — Backend Dependencies
# ============================================================
# This file lists all Python packages required for the backend.
# Install with: pip install -r requirements.txt
# ============================================================

# --- Web Framework ---
# FastAPI: Modern, high-performance web framework for building APIs
# Uvicorn: ASGI server to run our FastAPI application
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# --- Environment & Configuration ---
# python-dotenv: Loads environment variables from .env file
# This keeps API keys and secrets out of source code
python-dotenv>=1.0.0

# --- HTTP Client ---
# httpx: Async HTTP client for calling external APIs (Semantic Scholar)
# Chosen over 'requests' because it supports async/await natively
httpx>=0.27.0

# --- Database (Neo4j) ---
# neo4j: Official Python driver for Neo4j graph database
# Used to store papers as nodes and citation relationships as edges
neo4j>=5.17.0

# --- AI / LLM Framework ---
# langchain-core: Core abstractions for LLM chains and tools
# langgraph: Framework for building stateful, multi-step AI agents
# langchain-google-genai: Google Gemini LLM integration for LangChain
langchain-core>=0.3.0
langgraph>=0.2.0
langchain-google-genai>=2.0.0

# --- Data Validation ---
# pydantic: Data validation using Python type annotations
# Already included with FastAPI, but listed explicitly for clarity
pydantic>=2.6.0

# --- SSE (Server-Sent Events) ---
# sse-starlette: Adds SSE support to FastAPI/Starlette
# Used to stream real-time agent progress updates to the frontend
sse-starlette>=2.0.0

# --- Testing ---
pytest>=8.0.0
pytest-asyncio>=0.23.0
